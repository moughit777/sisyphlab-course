name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 6 */5 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s -o /dev/null -w "Status: %{http_code}\n" \
            "https://griszqwabfhscoslavgx.supabase.co/rest/v1/tokens?select=id&limit=1" \
            -H "apikey: sb_publishable_COunu3zCz6gmEh-s29qxDw_fyaRVKLg"
